message.getAllByChatId=select id, chat_id, author_id, content, date \
from \"Message\" \
where chat_id = cast(? as uuid)
message.create=insert into \"Message\" \
  (id, chat_id, author_id, content, date) \
  values \
  (uuid_generate_v1(), cast(? as uuid), cast(? as uuid), ?, now())